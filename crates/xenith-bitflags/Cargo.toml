[package]
name = "xenith-bitflags"
version = "0.1.0"
edition = "2021"
authors = ["Xenith OS Contributors"]
license = "MIT OR Apache-2.0"
description = "A tiny no_std bitflags! macro for Xenith OS"
publish = false

[lib]
path = "src/lib.rs"

[features]
default = []
